return {
    git = {
        enable = true,
    },
    view = {
        side = "left",
    },
    renderer = {
        -- root_folder_label = true,
        highlight_opened_files = "all",
        indent_markers = {
            enable = true,
        },
        highlight_git = true,
        icons = {
            show = {
                git = true,
            },
        },
    },
}
